// Example snippet to add/override superpowers plugin config.
// Use `openclaw config set ...` if you prefer CLI edits.
{
  plugins: {
    entries: {
      superpowers: {
        enabled: true,
        config: {
          enabled: true,
          autoSelect: true,
          injectSkillText: true,
          showTip: true,
          maxInjectedChars: 9000,
          defaultSkill: "using-superpowers",
        },
      },
    },
  },
}

